import 'package:flutter/material.dart';

class AppColors {
  // ─── Primary — 森林绿 (Forest Green) ───
  static const primary = Color(0xFF2D6A4F);       // 沉稳森林绿
  static const primaryDark = Color(0xFF52B788);    // 暗色模式提亮变体
  static const primaryLight = Color(0xFFE8F5EE);   // 浅底色背景

  // ─── Accent — 玫瑰金 (Rose Gold) ───
  static const accent = Color(0xFFD4816B);         // 温暖玫瑰金
  static const accentLight = Color(0xFFFBEDE7);    // 浅底色

  // ─── Backgrounds ───
  static const background = Color(0xFFF8F7F5);     // 暖白纸质感
  static const surface = Color(0xFFFFFFFF);

  // ─── Text — 4 级灰度 ───
  static const textPrimary = Color(0xFF1A2421);    // Deep green-black
  static const textSecondary = Color(0xFF5C6B64);  // Mid green-grey
  static const textTertiary = Color(0xFF9AA89F);   // Light green-grey
  static const textDisabled = Color(0xFFC8D0CC);   // Disabled

  // ─── Divider / Border ───
  static const divider = Color(0xFFE3E8E5);
  static const border = Color(0xFFCED6D1);

  // ─── Semantic ───
  static const success = Color(0xFF52B788);
  static const warning = Color(0xFFE6A650);
  static const error = Color(0xFFD45454);
  static const info = Color(0xFF4A9EC4);

  // ─── Progress Bars ───
  static const progressGreen = Color(0xFF52B788);
  static const progressYellow = Color(0xFFE6C84D);
  static const progressOrange = Color(0xFFD4816B);
}
